# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
lib64/
!*/src/lib/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyTest
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
venv/
.venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp.terraform/
*.tfstate
*.tfstate.backup
.terraform/
**/.terraform/
terraform.tfstate
terraform.tfstate.*
*.tfstate.*
.terraform.lock.hcl
**/.terraform.lock.hcl
*.tfvars
*.tfvars.json

# Local environment secrets
.env
*.env

# Mailserver runtime artifacts
services/mailserver/data/
services/mailserver/logs/
services/mailserver/backups/

# Mailserver secrets and authentication (NEVER commit these)
services/mailserver/config/postfix/sasl_passwd
services/mailserver/config/postfix/sasl_passwd.db
services/mailserver/config/dovecot/users

# Dovecot SQL configuration with credentials
services/mailserver/config/dovecot/dovecot-sql.conf.ext

# Staging environment runtime and secrets
services/mailserver/data-staging/
services/mailserver/logs-staging/
services/mailserver/.env.staging
services/mailserver/config-staging/

# Terraform artifacts (plan files can contain sensitive data)
**/tfplan
**/*.tfplan
services/mailserver/terraform/*.tfplan
services/mailserver/terraform/staging.tfplan
services/mailserver/terraform/s3-backup/tfplan

# Claude Code settings with sensitive data
.claude/settings.local.json

# Backup files with credentials
*.backup
services/mailserver/.env.phase*.backup
services/mailserver/docker-compose.yml.phase*.backup

# Playwright MCP screenshots
.playwright-mcp/

# Serena MCP configuration
.serena/

# All documentation except top-level README.md (keep locally, not public)
docs/

# Service documentation and internal files
services/**/*.md
services/**/docs/

# Cloudflare Workers and Wrangler (NEVER commit these)
.wrangler-config/
**/.wrangler/
cloudflare/**/.env
cloudflare/**/node_modules/
services/unified-portal/frontend/node_modules/
.claude/
CLAUDE.md
AGENTS.md